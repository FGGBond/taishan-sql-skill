# taishan-sql CLI

Bundled CLI for the Taishan SQL skill.

Install from the skill root:

```bash
bash scripts/install.sh
```

Or install this CLI package directly:

```bash
python -m pip install -e .
```
